node_modules/
dist/
*.log
# verification screenshots
shot-*.png
artifacts/
# generated drift-audit report + scratch
audit-report.md
.audit/
# local agent observability log (hook events)
.claude/logs/

# Secrets / credential carriers — never commit
.env
.env.*
*.pem
*.key
*.p12
*.keystore
id_rsa*
credentials*
*.tfvars

# Personal / Drive tier — must never reach GitHub (covers PERSONAL_JOURNAL.md)
PERSONAL_JOURNAL*
private/
